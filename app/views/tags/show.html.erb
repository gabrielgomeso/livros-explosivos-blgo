<p id="notice"><%= notice %></p>

<p>
  <strong>Publicações com a tag <span class="is-italic">
 <%= @tag.name %> </strong>
  

  <ul>
    <% @posts.each do |post| %>
      <li> <%= post.title %> </li>
    <% end %>
  </ul>
</p>

<%= link_to 'Edit', edit_tag_path(@tag) %> |
<%= link_to 'Back', tags_path %>
